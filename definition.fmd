? options

? defaults
	optional/required

? page 0: ## page title

? bool bool_name = true
? str str_name = "str_value" // explanation
// ? num num_name = num_value (won't be rendered)

? 0: ### checkboxes
	min=1,max=2
	required/optional
	hidewhen=bool_name
	> description
	>
	> with multiple lines
	[] 0: option 1
		paragraph
	[x] 1: option 2

? 1: ### multiple-choice / radio buttons*
	() option 1
		paragraph
	(x) option 2

? 3: ### short paragraph*
	type=[*text*/date/number/email/area]
	value=,placeholder=
	min=,max=

? page 1: ## page title